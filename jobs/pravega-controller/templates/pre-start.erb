#!/bin/bash

export LOG_DIR=/var/vcap/sys/log/pravega-controller
export RUN_DIR=/var/vcap/sys/run/pravega-controller

for dir in $LOG_DIR $RUN_DIR; do
  mkdir -p ${dir}
  chown vcap:vcap ${dir}
  chmod 700 ${dir}
done
