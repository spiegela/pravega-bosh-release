set -e
set -u

export JAVA_HOME=/var/vcap/packages/jdk8/jdk
export PATH=$JAVA_HOME/bin:$PATH

pushd pravega-benchmark
./gradlew build
tar -xvf ./build/distributions/pravega-benchmark.tar -C $BOSH_INSTALL_TARGET
popd

chown -R vcap:vcap $BOSH_INSTALL_TARGET
